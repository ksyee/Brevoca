# Brevoca prompts

Prompt assets are versioned outside application code so API and worker can
reference the same template set.

- `templates/general-meeting-summary.md`: generic office meeting format
- `templates/manufacturing-minutes.md`: production and field meeting format

Keep prompt versions alongside release notes once summary generation is wired.
